node_modules/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/
/.playwright-cli/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Raw Greenbook source snapshots — archived to object storage, not committed.
.greenbook-snapshots/

# WorkBuddy AI workspace state (memory, session data) — not project source.
.workbuddy-ai/

# Stray empty files left by sandboxed shells.
_tmp_*

# Phase 6 knowledge-seed HTML snapshots — runtime artifacts written by
# scripts/phase6/knowledge-seed.mjs, regenerated on every live run. Retained
# locally for review; not committed (same policy as .greenbook-snapshots/).
seed/phase6/snapshots/
